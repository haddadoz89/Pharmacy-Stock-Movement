<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

<nav class="navbar" style="background-color: #002b5c;">
  <div class="container-fluid d-flex justify-content-between">
    <a class="navbar-brand text-white" href="/">Pharmacy Stock Movement</a>

    <div class="d-flex align-items-center gap-3">
      <% if (user) { %>
        <a class="nav-link text-white" href="/medications">Medications</a>
        <a class="nav-link text-white" href="/expiry-check">Expiry Check</a>
        
        <% if (user.position === "Head of Pharmacy") { %>
              <a class="nav-link text-white" href="/users">User Management</a>
          <% } %>
    </div>
          <span class="navbar-text text-white">
          Logged in as <strong><%= user.username %></strong>
          (<%= user.position %>)
          <% if (user.healthCenter) { %>
            - <%= user.healthCenter.healthCenterName %>
          <% } %>
        </span>

        <a class="btn btn-outline-light" href="/auth/logout">Logout</a>
      
      <% } else { %>
        <a class="btn btn-outline-light" href="/auth/sign-in">Sign In</a>
      <% } %>
    </div>
</nav>